---
title: My Blog
pagination:
  for: posts
  per_page: 10
---

<article class="wrapper post">
	<header class="header">
		<h3 class="bold">nachos</h3>
		<hr>
		<p class="italic">guac</p>
	</header>
	<h2>we eat nachos. <br>then tell you how much we like them.</h2>
	<br>
	<ul>
	  <% pagination.each do |post| %>
	  <a style="color: #fff" href="/blog/<%= post['slug'] %>/">
	  	<div class="posts">
			<li><%= post['title'] %></li>
		</div>
	  </a>
	  <% end %>
	</ul>

	<div class="pager">
	  <%= link_to "Prev page", pagination.prev_page.url, class: 'previous' if pagination.prev_page %>
	  <%= link_to "Next page", pagination.next_page.url, class: 'next' if pagination.next_page %>
	</div>
</article>
